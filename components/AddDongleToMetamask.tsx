import React from 'react'

import Button from "react-bootstrap/Button";
import "bootstrap/dist/css/bootstrap.min.css";
import styles from "./AddDongleToMetamask.module.scss";

const AddDongleToMetamask = () => {
    const tokenAddress
    const tokenSymbol
    const tokenDecimals
    const tokenImage
   async (params:type) => {
       
   }
  return (
      
    <div>AddDongleToMetamask</div>
  )
}

export default AddDongleToMetamask